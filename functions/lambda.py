# Python lambdas are little, anonymous functions, subject to a more restrictive but more concise syntax
# than regular Python functions.

result = (lambda x, y, z: x + y + z)
print('print result:', result(1, 2, 3))
